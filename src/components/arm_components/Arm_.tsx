import React from "react";

import Arm from "./Arm";

const Arm_ = () => {

    return(
        <Arm pageNumber="2">
            <div style={{display:"flex", alignItems:'center', justifyContent:'center',width:'100%',height:'100%'}}>Arm_</div>
        </Arm>
    );
}

export default Arm_;