import React from "react";

import Arm from "./Arm";

const Arm_credits = () => {

    return(
        <Arm pageNumber="-1">
            <div style={{display:"flex", alignItems:'center', justifyContent:'center',width:'100%',height:'100%'}}>Arm_credits</div>
        </Arm>
    );
}

export default Arm_credits;