import React from "react";

import Arm from "./Arm";

const Arm_login = () => {

    return(
        <Arm pageNumber="-2">
            <div style={{display:"flex", alignItems:'center', justifyContent:'center',width:'100%',height:'100%'}}>Arm_login</div>
        </Arm>
    );2}

export default Arm_login;